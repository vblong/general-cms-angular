import { NewEntryRoutingModule } from './new-entry-routing-module';

describe('NewEntryRoutingModule', () => {
  it('should create an instance', () => {
    expect(new NewEntryRoutingModule()).toBeTruthy();
  });
});
